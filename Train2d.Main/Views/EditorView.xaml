<UserControl x:Class="Train2d.Main.Views.EditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Train2d.Main.Views" 
             xmlns:viewmodel="clr-namespace:Train2d.Main.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance viewmodel:MainWindowViewModel}">
  <StackPanel>
    <CheckBox Content="Valid Position" IsChecked="{Binding Path=SelectedView.EditorController.ValidPosition}"/>
    <CheckBox Content="Place Track" IsChecked="{Binding Path=SelectedView.EditorController.PlaceTracks}"/>
    <GroupBox IsEnabled="{Binding Path=SelectedView.EditorController.PlaceTracks}">
      <StackPanel>
        <RadioButton Content="Horizontal" Grid.Row="0" Grid.Column="1" IsChecked="{Binding Path=SelectedView.EditorController.Horizontal}"/>
        <RadioButton Content="Vertical" Grid.Row="0" Grid.Column="1" IsChecked="{Binding Path=SelectedView.EditorController.Vertical}"/>
        <RadioButton Content="Diagonal" Grid.Row="0" Grid.Column="1" IsChecked="{Binding Path=SelectedView.EditorController.Diagonal}"/>
        <RadioButton Content="Antidiagonal" Grid.Row="0" Grid.Column="1" IsChecked="{Binding Path=SelectedView.EditorController.AntiDiagonal}"/>
      </StackPanel>
    </GroupBox>


  </StackPanel>
</UserControl>
